extends common/layout

block layout-content
  .container
    h1 Confirm Delete
    p Are you sure? you want to delete this item?

    form(action="/admin/tour/delete/submit" method="post")
      input(type="hidden" name="packageId" value=packageId)
      button#cancel-btn(type="button" onclick="window.history.back();") Cancel
      button#ok-btn(type="submit") Ok